#include <iostream>
using namespace std;

int main()
{

    int C = 523;
    int  D = 587;
    int E = 659;
    int F = 698;
    int G = 740;
    int A = 880;
    int B = 988;


	char h = '&';

	//Sleep(3002);

	//system("cls");

	//Sleep(500);


	for (int i = 0; i < 40; i++) {
		cout << "_";
	}
	//Sleep(500);


	cout << endl << endl << "\t\t\t\t  i i i i i" << endl;
	// Sleep(500);
	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(D, 340);
	// Beep(C, 382);
	// Beep(F, 286);
	// Beep(E, 304);
	cout << "\t\t\t\t  i i i i i" << endl;
	//Sleep(500);

	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(D, 340);
	// Beep(C, 382);
	// Beep(G, 270);
	// Beep(F, 286);

	cout << "\t\t\t\t__i_i_i_i_i__";
	// Sleep(500);

	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(A, 228);
	// Beep(F, 286);
	// Beep(E, 304);


	cout << endl << "\t\t\t       |" << h << "\t    " << h << "|";
	// Sleep(500);
	// Beep(D, 340);
	// Beep(B, 220);
	// Beep(B, 220);
	// Beep(A, 228);
	// Beep(F, 286);
	// Beep(G, 290);
	// Beep(F, 286);
	// Beep(E, 304);


	cout << endl << "\t\t\t     __| " << h << "         " << h << " |__";
	// Sleep(500);

	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(D, 340);
	// Beep(C, 382);
	// Beep(F, 286);
	// Beep(E, 304);

	cout << endl << "\t\t\t    | " << h << "    " << h << "     " << h << "    " << h << " |";
	// Sleep(500);

	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(D, 340);
	// Beep(C, 382);
	// Beep(G, 270);
	// Beep(F, 286);

	// cout << endl << "\t\t\t  __|" << h << "   HAPPY   " << h << "     " << h << "|__";
	// Sleep(500);

	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(C, 382);
	// Beep(A, 228);
	// Beep(F, 286);
	// Beep(E, 304);

	cout << endl << "\t\t\t | " << h << "  BIRTHDAY    " << h << "      " << h << " |";
	// Sleep(500);
    // Beep(D, 340);
	// Beep(B, 220);
	// Beep(B, 220);
	// Beep(A, 228);
	// Beep(F, 286);
	// Beep(G, 290);

	cout << endl << "\t\t\t |" << h << " ARCHEEL   " << "   " << h << "  " << h << "  " << h << "  " << h << "|";
	//Sleep(500);
	
	cout << endl << "\t\t\t | " << h << "  " << h << "            " << h << "     " << h << " |";
	//Sleep(500);
	


}
